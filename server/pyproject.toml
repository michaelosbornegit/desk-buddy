[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "desk-buddy-server"
version = "0.0.1"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]